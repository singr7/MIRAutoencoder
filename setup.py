from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Setting up a project to create data science pipeline',
    author='Ravinder',
    license='MIT',
)
